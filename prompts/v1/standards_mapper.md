# StandardsMapper — v1

Placeholder prompt. Real prompt content to be authored once FR-1
(standards ingestion) and the gap-analysis use case are implemented.

Prompts are versioned files under `prompts/`, never inline string
literals in application code — this lets prompt changes be reviewed,
diffed, and rolled back independently of code changes.