"""
Seed/ingest script placeholder.

This will eventually run the FR-1 ingestion pipeline (extract, clean,
chunk, embed, index) against the D3 corpus. Currently a no-op so the
required `docker compose up` + seed command pairing exists from day one.
"""

def main():
    print("Seed script placeholder — ingestion pipeline not yet implemented.")

if __name__ == "__main__":
    main()